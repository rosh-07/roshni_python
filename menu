def get_menu(choice):
  menu = {
    1:insert,
    2:delete,
    3: update
    4:list
    5:exit
  }
  return menu[choice]

def run_menu():
  while true:
    print('\n 1.insert 2.delete 3.update 4.list 5.exit')
    choice = int(input('your choice plz:'))
    get_menu(choice)
    if choice==5:
      break
    
